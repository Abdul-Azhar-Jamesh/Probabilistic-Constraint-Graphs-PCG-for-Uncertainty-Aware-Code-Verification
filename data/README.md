# Data

This directory is reserved for project datasets and processed artifacts.

- `raw/`: source data, benchmark artifacts, and calibration input files.
- `processed/`: cleaned or transformed datasets used by training and evaluation.
