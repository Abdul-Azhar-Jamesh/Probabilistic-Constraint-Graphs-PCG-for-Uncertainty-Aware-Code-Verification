"""Minimal streamlit app entry point for the PCG project."""

import streamlit as st

st.title("PCG")
st.write("Probabilistic Constraint Graphs for uncertainty-aware code verification.")
st.caption("This project currently runs as a Python package with CLI and calibration tooling.")
